# X-Term 产品使用文档

> **一站式运维工作站** —— SSH 终端、SFTP 文件传输、MySQL 数据库、远程桌面、AI 智能助手、MCP 服务，全部集成在一个轻量桌面应用里。

---

## 一、产品简介

X-Term 是一款面向**运维工程师、后端开发者和 DBA** 的现代化桌面运维客户端。它将日常运维工作流中分散在多个工具里的能力整合进一个应用：不再需要同时开着 XShell、WinSCP、Navicat、远程桌面、密码管理器……一个 X-Term 全部搞定。

### 产品亮点（核心价值）

| 价值点 | 说明 |
|---|---|
| **一站式** | 终端、文件传输、数据库、远程桌面、端口转发、密钥保险库六大能力合一，省去多工具来回切换 |
| **AI 智能化** | 不只是聊天，而是能"动手干活"的智能体——替你执行命令、查数据库，且**每次执行都经你确认** |
| **安全可信** | 凭据 AES-256-GCM 加密、主密码保护；危险命令自动识别拦截；MCP 外部调用强制人工审批 |
| **极轻量** | 基于 Tauri 2（Rust 核心）构建，安装包小、启动快、内存占用低，远轻于 Electron 类应用 |
| **现代化体验** | 多标签终端、拖拽传输、暗色主题、快捷键自定义、命令面板，如 IDE 般顺手 |
| **可扩展** | 通过标准 MCP 协议把能力开放给 Claude Desktop、Cursor 等外部 AI 客户端 |

---

## 二、安装与启动

### 环境要求

- Windows 10 / 11（64 位）

### 安装步骤

1. 下载安装包 `X-Term_x.x.x_x64-setup.exe`
2. 双击运行，按提示完成安装
3. 从桌面快捷方式或开始菜单启动 X-Term

### 首次启动：创建凭据保险库

首次打开应用会引导你**设置主密码**，用于加密保存所有服务器密码、私钥、数据库密码等敏感凭据。

> ⚠️ **重要**：主密码不存盘、无法找回。请务必牢记！丢失主密码等于丢失全部凭据，只能重新录入。

主密码至少 6 位，建议使用高强度口令并妥善保管。之后每次启动应用需先解锁保险库；也可以手动锁定（不影响已建立的连接）。

---

## 三、界面总览

启动并解锁后，左侧为**功能导航栏**：

| 导航 | 功能 |
|---|---|
| 终端 | SSH / Telnet / 本地终端，多标签会话 |
| SFTP | 与服务器之间的文件上传/下载管理 |
| 文件 | S3 / 兼容对象存储（MinIO 等）文件浏览器 |
| DB | MySQL 数据库控制台 |
| 转发 | 端口转发规则（本地 / 远程 / SOCKS5） |
| 桌面 | RDP / VNC 远程桌面 |
| 密钥 | 密钥保险库（密码、私钥管理） |
| MFA | TOTP 动态验证码管理 |
| MCP | MCP 服务端（对外开放给外部 AI 客户端） |
| 设置 | 终端主题、AI 配置、快捷键等 |

---

## 四、功能使用指南

### 1. 终端（SSH / Telnet / 本地）

**新建连接**

1. 进入「终端」页，点击会话树上方 **新建会话**
2. 填写名称、主机、端口、用户名
3. 选择认证方式：**密码** 或 **私钥**（可关联保险库凭据）
4. 保存后**双击会话**即可连接；支持 `ssh` / `telnet` 协议，也支持配置**跳板机**链路

**多标签管理**

- 一个服务器一个标签，像浏览器一样多标签切换（`Ctrl+Tab` / `Ctrl+Shift+Tab`）
- 标签右键：**复制会话**（同配置再开一个）、关闭、关闭其他、全部关闭、重连
- 会话树支持**分组**，会话可拖拽移动到不同分组；侧栏宽度可拖拽调整，可折叠
- 顶部工具栏：清屏、字体放大/缩小、断线一键重连

**终端体验**

- **选中即复制**，右键直接粘贴（PuTTY 风格，可在设置中切换）
- `Ctrl+F` 终端内搜索；连接断开自动提示，一键重连
- 支持**本地终端**（cmd / PowerShell / Git Bash，可配置默认 shell）
- URL 自动可点、主题与字体可配、WebGL 加速渲染

**快捷命令栏**

终端底部常驻快捷命令栏：把高频命令（如 `df -h`、`tail -f /var/log/xxx`）存成按钮，点击一键发送；支持**分组标签页**管理、支持 `{host}/{user}/{port}` 占位符自动替换、可绑定自定义快捷键、可将当前输入的命令一键保存为快捷命令。

### 2. SFTP 文件传输

1. 进入「SFTP」页，选择已连接的会话
2. 界面为**本地 / 远程双栏**布局，支持面包屑导航、列排序
3. **直接拖拽**：把本地文件拖到远程栏即上传，远程文件拖到本地栏即下载
4. 支持多文件批量传输、**断点续传**、新建目录、重命名、删除、改权限
5. 底部**传输队列**实时显示进度与速度

### 3. 文件存储（S3 对象存储）

「文件」页支持连接 **S3 及兼容对象存储**（AWS S3、MinIO、COS、OSS 等，SigV4 认证）：

1. 新建账号：填写 Endpoint、Region、Bucket、AccessKey / SecretKey（凭据加密保存）
2. 支持 path-style / virtual-hosted-style 两种寻址风格
3. 连接后本地与远端双栏浏览，支持上传、下载、新建目录、重命名、删除
4. 传输进度复用统一的传输队列展示

### 4. MySQL 数据库控制台

1. 进入「DB」页，**新建连接**：填写主机、端口、用户、密码；支持**直连**或**经 SSH 隧道**连接
2. 左侧为**数据库树**（分组 → 实例 → 库 → 表），点击表名自动生成 `SELECT` 并补全字段
3. 两种执行模式：
   - **命令行模式**：类 mysql CLI，回车执行、历史保留
   - **代码模式**：多行编辑器，**表格化结果展示**，结果可导出 **CSV / JSON**
4. 支持 SQL 自动补全、语法高亮、连接分组管理（创建/重命名/删除）

### 5. 远程桌面（RDP / VNC）

1. 进入「桌面」页，新建 RDP 或 VNC 连接（密码加密存储）
2. 连接方式二选一（可在设置中配置）：
   - **程序内嵌**：无需安装任何客户端，直接在应用内标签页打开桌面
   - **系统客户端**：RDP 调起 `mstsc`，VNC 调起 `vncviewer`
3. 支持多桌面标签页同时打开、分组管理连接

### 6. 端口转发

「转发」页支持三类端口转发规则，规则**持久化保存、按需启停、可自动启动**：

| 类型 | 说明 | 适用场景 |
|---|---|---|
| 本地转发（-L） | 把远程服务映射到本地端口 | 本地访问服务器上的内部服务 |
| 远程转发（-R） | 把本地服务暴露到远程 | 把内网服务开放给远端 |
| 动态转发（-D） | SOCKS5 代理 | 浏览器走代理访问内网 |

### 7. 密钥保险库

「密钥」页集中管理所有凭据：

- 支持添加密码、SSH 私钥等条目
- 凭据使用 **AES-256-GCM 加密存储**，运行时内存解密，主密码不落盘
- 会话、数据库、文件存储、桌面连接均可关联保险库凭据，一处录入多处使用

### 8. MFA 动态验证码（TOTP）

「MFA」页管理两步验证码：

- 添加方式：手动输入 issuer / account / secret，或**直接粘贴 `otpauth://` URI 自动解析**
- 列表实时展示动态码与倒计时，支持复制、删除
- **一键填充**：把验证码直接填入当前活动的终端会话（SSH 登录二次验证场景）

### 9. AI 智能助手 ⭐ 核心差异化功能

X-Term 的 AI 不只是问答，而是**能调用工具、执行运维操作**的智能体。

**配置**

1. 打开「设置 → AI 助手」，添加 API Key
2. 支持多家模型：**OpenAI / DeepSeek / 智谱 / Ollama / 任意 OpenAI 兼容服务**，可配置多套并在会话中切换
3. 终端、数据库、桌面三处各有独立 AI 助手面板

**两种模式**

- **对话模式**：问答、翻译、代码诊断、配置解释
- **智能体模式**：AI 自动规划并执行任务，例如：

```
你：看看这台机器磁盘满没满
AI：我来检查磁盘使用情况。
    [工具调用] exec_ssh: df -h        [执行] [拒绝]
你：点「执行」
AI：根分区 / 使用 45%（可用 12G），未满。/data 用了 87%，接近告警线。
    建议进一步查看 /data 下的大文件吗？
```

**智能体能做什么**

- 在服务器上执行 shell 命令（`exec_ssh`），可实时回显到终端
- 读取终端上下文，AI 知道屏幕上的内容（`terminal_snapshot`）
- 查询 MySQL 数据库、列出库表、查看表结构（`exec_sql`）
- 多轮编排：AI 可连续执行多个工具完成任务（如"先看磁盘 → 找大文件 → 列出 top10"）

**安全机制（重要）**

- **人确认执行**：所有工具调用默认先弹确认卡片，你点「执行」才真正执行，AI 无法绕过
- **危险操作拦截**：`rm -rf /`、格式化磁盘、重启关机、无 WHERE 的 DELETE 等自动识别并红色高亮 + 强制确认
- **白名单自动放行**：可配置命令白名单，常用安全命令免确认；SQL 默认只读模式（可选受限写 / 自由模式）
- 单命令 30 秒超时、输出自动截断、单次对话最多 10 轮工具调用、5 分钟不确认自动拒绝

### 10. MCP 服务端

将 X-Term 的执行能力通过**标准 MCP 协议（Model Context Protocol）**暴露给外部 AI 客户端（Claude Desktop、Cursor、Windsurf 等），让外部 AI 直接操作你的服务器和数据库。

提供两个独立实例：

| 实例 | 暴露工具 | 说明 |
|---|---|---|
| **SSH MCP** | `exec_ssh(command)` | 在绑定的 SSH 会话上执行命令 |
| **DB MCP** | `exec_sql(sql)` | 在绑定的数据库连接上执行 SQL |

使用方法：

1. 进入「MCP」页，启动实例并获取 **Bearer Token**
2. 服务默认绑定 `127.0.0.1`，仅在本地监听
3. **一键复制**客户端配置 JSON，粘贴到 Claude Desktop / Cursor 等客户端
4. 外部 AI 调用时，应用内弹出**审批浮层**，你确认后才执行；可开启自动放行（适合受信环境）
5. 内置实时执行日志面板，全程可追溯

### 11. 设置

- **终端**：主题、配色方案、字体、字号、滚动回看行数、右键粘贴、WebGL 渲染、SSH 空闲超时 / 保活 / 连接超时
- **AI 助手**：模型供应商、API Key、运行模式、SQL 模式、白名单、文件读写授权
- **快捷键**：全部应用级快捷键可自定义（新建终端、关闭、切换、搜索、AI 面板等）
- **连接**：桌面客户端模式（内嵌 / 系统客户端）、RDP 证书校验
- **关于**：应用信息、数据目录、**检查更新**

### 12. 应用自更新

「设置 → 关于」中可一键检查更新：自动拉取版本清单，发现新版后流式下载安装包（实时进度 + sha256 完整性校验），确认后自动拉起安装器完成升级。更新源地址可配置。

---

## 五、常用快捷键速查

| 快捷键 | 功能 |
|---|---|
| `Ctrl+T` | 新建终端（快速连接最近会话） |
| `Ctrl+W` | 关闭当前终端 |
| `Ctrl+Tab` / `Ctrl+Shift+Tab` | 切换终端标签 |
| `Ctrl+1~9` | 切换到第 N 个标签 |
| `Ctrl+Shift+C` / `Ctrl+Shift+V` | 终端复制 / 粘贴 |
| `Ctrl+F` | 终端内搜索 |
| `Ctrl+I` | 展开 / 收起 AI 助手面板 |
| `Ctrl+P` | 聚焦会话树搜索框 |
| 选中文本 | 自动复制（可在设置中关闭） |

> 全部快捷键可在「设置 → 快捷键」中自定义。

---

## 六、安全设计

- **凭据加密**：所有密码/私钥用主密码（Argon2id 派生）+ AES-256-GCM 加密后落盘，主密码永不存盘
- **保险库锁定**：可手动锁定应用，锁定时清空内存中的主密钥
- **known_hosts 校验**：首次连接记录服务器指纹，指纹变更弹窗提醒，防中间人攻击
- **AI 权限最小化**：默认人工确认执行、危险命令强制二次确认、SQL 默认只读
- **MCP 访问控制**：仅本地监听 + Token 认证 + 人工审批

---

## 七、数据存储位置

- 应用数据目录：`%APPDATA%\x-term\`
  - `xterm.db`：会话、分组、转发规则、数据库配置等
  - `settings.json`：全局设置
  - `credentials.enc`：加密后的凭据文件

---

## 八、常见问题

**Q：忘记主密码怎么办？**
A：无法找回。主密码只存在你脑中，请务必妥善保管；如遗忘只能删除数据目录重新初始化。

**Q：支持哪些系统？**
A：当前支持 Windows 10 / 11（64 位），安装包为 exe（MSI / NSIS 两种安装格式）。

**Q：AI 智能体需要额外付费吗？**
A：X-Term 本身免费；AI 能力使用你自己的 API Key（BYOK），按各家模型服务的定价计费。

**Q：如何让外部 AI（Claude / Cursor）操作我的服务器？**
A：打开「MCP」页启动实例，复制配置 JSON 粘贴到客户端即可，调用时应用内会弹出确认。

**Q：数据库如何走 SSH 隧道连接？**
A：新建数据库连接时选择"经 SSH 隧道"，并关联一个已配置的 SSH 会话即可，无需额外工具。

---

*X-Term 让运维更简单、更安全、更智能。*
